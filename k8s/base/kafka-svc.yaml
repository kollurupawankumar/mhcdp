apiVersion: v1
kind: Service
metadata:
  name: kafka
spec:
  selector:
    app: kafka
  ports:
    - name: kafka-internal
      port: 29092
      targetPort: 29092
    - name: kafka-host
      port: 9092
      targetPort: 9092
  type: ClusterIP
